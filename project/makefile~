CMP = g++
MAIN = animate
FUNC = LTexture
FLAGS = -lSDL2 -lSDL2_image

all: $(MAIN)

#$(MAIN): $(MAIN).o
#	$(CMP) $(MAIN).o -o $(MAIN) $(FLAGS)

$(MAIN): $(MAIN).cpp
	$(CMP) $(MAIN).cpp -o $(MAIN) $(FLAGS)

#$(FUNC).o: $(FUNC).cpp
#	$(CMP) -c $(FUNC).cpp $(FLAGS)

clean:
	rm *.o $(MAIN)
